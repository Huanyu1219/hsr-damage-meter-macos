# 项目开发准则

- 所有设计、代码、文档与配置以仓库根目录就地构建；开始操作前确认工作目录。
- 使用中文简洁沟通。每次交互仅推进一个独立子任务，完成后给出产物与验证步骤。
- 用户明确禁止 Computer Use；仅使用代码检查、自动化测试和必要的离屏渲染，不操作或捕获用户屏幕。
- 用户希望自主完善并测试后直接交付；UI仅保留有用指标、状态和操作，去掉开发说明与重复标题，优先采用本地角色头像。参考Veritas与其他GitHub项目的信息结构，保留来源记录。
- 规范优先：功能实现前确立目录、分支、Conventional Commits、API 契约与编码风格。
- `CODEX_SPEC.md` 为用户指定的权威工程与 UI 规范。当前路线已确定为 Veritas Collector + Rust 协议桥 + Swift 6 原生 macOS 客户端；许可证待上游核验和用户确认。
- 存在规范未确定的多种实现路线时，简述核心权衡、给出推荐，待用户确认后执行。
- 用户已验证 Method 2 加载成功，并明确同意复用现有 Socket.IO（127.0.0.1:1305），覆盖原规范的新增 Publisher 顺序；决定见 docs/adr/0001-reuse-veritas-socketio.md。
- 当前游戏配置为 4.5.0，现有 Collector 0.2.52 发布说明面向 4.3.54+，兼容性和统计完整性尚未验证。用户反馈不加载不卡、隐藏 UI 仍卡，优先定位兼容性与性能，不将收到事件等同统计正确。
- 用户进一步授权先做原生 Mac UI，再禁用原 DLL 的 UI 初始化，进行单变量对照；见 docs/adr/0002-native-ui-overlay-isolation.md。无 UI DLL 已安装并保留原版备份，实际战斗效果待用户验收。
- 第一轮用户确认Mac有伤害但无UI版仍卡；第二轮用户退出游戏后已安装no-ui-info，关闭Debug/Trace但保留Info/Warn/Error，等待真实战斗性能反馈。原版及上一no-ui版本都有校验备份。
- 用户已确认no-ui-info不再卡顿。保持已验证DLL，本轮继续按Mac App里程碑完善界面和按需核验数据。
- 用户已授权进入 M7 历史和数据库，并选择跳过 M6 菜单栏扩展；分析和装备导入仍按独立里程碑推进。游戏运行时不替换 DLL；编译尽量限制并行度，避免干扰帧时间对比。
- 禁止硬编码密钥、令牌等敏感信息；初始化工程时配置 `.gitignore` 与不含真实凭据的 `.env.example`。
- 修改已有文件采用 Unified Diff 或明确的局部替换；禁止全量输出已有文件，不在单次交互生成多个长文件的实现。
- 开始任务先读 `.project-state.json`；仅按需读取特定接口、函数及局部代码，不要求用户重复粘贴未改动文件。
- 每次完成子任务更新状态索引，仅记录二级目录与关键模块、公共 API 签名、待办里程碑和已完成功能，不复制实现。

## 分阶段工程要求

1. 初始化与治理：评估并确认 MIT、Apache-2.0 或 GPLv3 等许可证；配置 LICENSE、README（定位、特性、架构、Quickstart、Demo）、CONTRIBUTING（环境、分支、提交、PR 与审核）、标准 Contributor Covenant 行为准则、SECURITY（报告渠道与支持版本）、Bug/Feature Issue 模板及 PR 模板。
2. 架构与基建：按领域划分 src、tests、docs、scripts；固化依赖锁文件、多环境规范、Linter、Formatter 与提交前 Git Hooks。
3. 核心研发：低耦合模块、类型约束与必要注释；测试驱动，配置单元测试、集成测试及覆盖率。
4. 自动化：PR 与主分支 Push 触发 CI，包含跨系统/运行时矩阵、Lint、SAST 与依赖检查；采用 SemVer、自动变更日志及目标平台构建发布流程。
5. 文档与社区：自动生成 API 文档、维护 ADR、提供最小可运行 examples 与上手教程。

按子任务逐步落实上述要求，不提前生成与未确认技术路线绑定的代码或配置。
