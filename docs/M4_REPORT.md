# M4 — 设计系统与实时面板

完成内容：统一间距/圆角与组件；三张指标卡；观察时长；角色头像与DPS；排行/可排序表格切换；
角色选择/详情；原始伤害类型分类表；离线/就绪/中断状态；缩写数字与精确值悬停；Reduce Motion 下关闭动画。
宽屏详情置右，窄屏置下；最低900×620，默认1120×760。没有实现History/Analysis假页面。

参考、数据口径与待确认项见 UI_REFERENCES.md。
本轮按用户要求停止 Computer Use，最终验证通过代码、测试和离屏 NSHostingView 渲染完成，
没有在新增表格/头像后进行交互式屏幕检查；实际操作与视觉观感可由用户反馈。

测试：Swift共19项（含一个真实本机WebSocket mock联调）通过；Rust原协议6项通过；
7 Schema、24非法样例及Rust↔Swift往返通过。普通无mock运行会显式跳过网络联调。
覆盖：规范6组数字例子、极大值、时长、两种占比分母、分类缺省/新标签/小数累计、
头像解码、演示重置，以及900×620离屏渲染。

产物：dist/HSR Damage Meter.app，本地ad-hoc签名。
复现构建：./scripts/build-macos-app.sh；测试：./scripts/check.sh。
本轮未改变游戏DLL，保持用户实测不卡顿的no-ui-info版本。

下一独立里程碑为M5原生悬浮窗（三种尺寸、鼠标穿透与位置保存）。历史/分析/数据库按规范后续推进。
